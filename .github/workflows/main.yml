name: Generate Podcast Feeds

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # 1. Checkout the repository
      - name: Checkout Repo
        uses: actions/checkout@v3
      - name: Run Feed Generator
        uses: kunalsarkar-bit/Podcast-generator-linkedin-learning@main

     
